# Runtime dependencies for the ExtMcp guardrail sidecar.
#
# Pinned where stability matters (grpc stack); the ML stack is pinned to known
# CPU-buildable versions. The container pre-downloads the PromptGuard-2 model
# at build time so the image is self-contained and never reaches HuggingFace at
# runtime.

grpcio==1.81.0
grpcio-tools==1.81.0
grpcio-health-checking==1.81.0
protobuf>=4.25,<8

# LlamaFirewall — semantic scanners (PromptGuard 2, AgentAlignment, CodeShield).
# Imported lazily; the package boots without it (regex-only mode) if absent.
llamafirewall>=0.9.0

# Transformers + CPU torch for PromptGuard-2-86M inference.
transformers>=4.45.0
torch==2.4.0

# Invariant Guardrails SDK (optional; the bundled rule engine in
# guardrails/invariant.py is the primary implementation, but invariant-ai
# provides richer rule DSLs if a deployment wants them).
invariant-ai>=0.0.4

# OpenTelemetry — only the SDK + OTLP exporter; the sidecar degrades to
# stdout-audit-only if these are absent.
opentelemetry-sdk==1.28.0
opentelemetry-exporter-otlp==1.28.0

# Small HTTP client for optional model-preflight / metrics push (kept minimal).
httpx==0.27.2
